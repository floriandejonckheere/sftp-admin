<!DOCTYPE html>
<html>
<head>
  <title>SftpAdmin</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav>
    <%= link_to dashboard_path, { :class => params[:controller].eql?('dashboard') ? 'active' : '' } do %>
      <div class="valign-middle">
        <i class="fa fa-dashboard"></i> Dashboard
      </div>
    <% end %>
    <%= link_to users_path, { :class => params[:controller].eql?('users') ? 'active' : '' } do %>
      <div class="valign-middle">
        <i class="fa fa-users"></i> Users
      </div>
    <% end %>
    <%= link_to shares_path, { :class => params[:controller].eql?('shares') ? 'active' : '' } do %>
      <div class="valign-middle">
        <i class="fa fa-archive"></i> Shares
      </div>
    <% end %>
    <%= link_to preferences_path, { :class => params[:controller].eql?('preferences') ? 'active': ''} do %>
      <div class="valign-middle">
        <i class="fa fa-gears"></i> Preferences
      </div>
    <% end %>
</nav>

<div class="container">
  <div class="panel panel-main">
    <%= yield %>
  </div>
</div>

</body>
</html>
